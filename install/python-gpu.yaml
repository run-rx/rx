image:
  # This corresponds to the image at
  # https://hub.docker.com/repository/runrx/conda.
  repository: 'runrx/conda'
  tag : 'v23.10.0'
remote:
  hardware:
    # This specifies that rx should choose a machine with a GPU.
    processor: 'gpu'
